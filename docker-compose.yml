services:
  wechat-photo-print:
    build: .
    image: wechat-photo-print-app:latest
    container_name: wechat-photo-print-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3231}:3230"
    volumes:
      - ./data:/data
      - ./downloads:/downloads
